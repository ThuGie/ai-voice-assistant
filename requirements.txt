# Core dependencies
numpy>=1.20.0
pillow>=8.3.1
requests>=2.25.0
tqdm>=4.62.0
pydub>=0.25.1
sounddevice>=0.4.4
python-dotenv>=0.19.0
typer>=0.4.0
rich>=10.9.0
scipy>=1.9.0  # For audio processing and filtering

# AI and ML dependencies
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
transformers>=4.25.1
accelerate>=0.19.0

# MeloTTS dependencies
melotts>=0.1.1  # Fixed version - 0.1.8 doesn't exist
librosa>=0.9.2

# Audio processing (for voice parameter adjustments)
# pyrubberband has dependencies on rubberband-cli which needs separate installation
# Comment out if causing installation issues
# pyrubberband>=0.3.0

# Faster-Whisper dependencies
faster-whisper>=0.8.0
ctranslate2>=3.10.0
ffmpeg-python>=0.2.0

# Ollama dependencies
# Note: You may need to install Ollama separately from https://github.com/ollama/ollama
requests>=2.25.0  # For API communication with Ollama

# Computer Vision dependencies
opencv-python>=4.6.0
mss>=6.1.0  # For screen capture
# Optional: Fast screen capture for Windows
# Comment out if causing installation issues on other platforms
# d3dshot>=0.14.4

# Natural Language Processing
nltk>=3.6.0  # For topic extraction and text analysis
scikit-learn>=1.0.0  # For text vectorization and similarity

# Additional utilities
# Comment out any that cause installation problems on your platform
pynput>=1.7.6  # For keyboard and mouse control
pyaudio>=0.2.11  # Audio recording
webrtcvad>=2.0.10  # Voice activity detection
psutil>=5.9.0  # For system monitoring (CPU, memory usage)
regex>=2022.3.0  # Enhanced regular expressions

# Optional - comment out if causing issues
# holidays>=0.14.0  # For holiday detection in context manager
# pyperclip>=1.8.0  # For clipboard integration

# GUI dependencies (optional - comment out if not needed)
# customtkinter>=4.6.3  # Modern looking tkinter
# PyQt5>=5.15.6  # Alternative GUI option
